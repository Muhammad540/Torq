# CORE  

## HIGH
[] Pinocchio integration
[] IK solver
[] YAML config loader
[] Collision Detector
[] RRTPlanner
[] HAL is needed, package Mujoco Driver using it sort of done
[] Path Planning
[] add control visualizer (to move each joints), enable/disable frames, enables/disable world frame axes
[] Use Null Space optimization 
[] Add Constraints to the IK solver, to stay close to the home position while moving
[] Halt if config already out of bounds (IK)
[] Fix toggle gripper to use the hardware interface abstraction instead of directly manipulatin the sim joints
[] make all the tasks/limits configurable (so the users can easily modify the targets/limits, weights.
[] Instead of silently fallbacking to ldlt, it would be better to return an error that should be handled by the user.
[] add check limits to IK solve (if the config is already out of bound). currently solve doesn't  check limits before building the IK.
[] currently gripper lock is hardcoded (name of gripper joint = 'gripper') which is excluded from IK computation, add a more configurable way to exclude joints 
[] define velocity limits so that the qp objective is minimized respecting the velocity limits (i observed that at the singularities (fully stretched) the robot shakes alot)
[] define/configure the freq at which high/low level controller runs
[] make the tasks weights configurable
[] Add more robot form factors (currently s0101 only) like franka, humanoids, spot
[] Add cam sensors (also incorporate into gui)
[] pybindings

## MEDIUM 
[] Dynamixel Driver
[] Fix the duplicate URDF/XML input to the RobotSystem (for mujoco and Pinocchio) i think both can work with either one of the them.
[] caching to reuse pose estiamtes btw frames
[] Behavior Tree Example
[] Display force contacts
[] Plot out the lowest singular value of J (to identify the singularities)
[] verify the warmstarting of the QP solver (to avoid recomputation every time)
[] return proper error codes from all cirtical methods (create a struct of all possible error codes, should be documented)
## LOW
[] startup is a bit slow, debug this.
[] add doxygen docs

## Done
[x] Fix the viewport position and size, currently it is not properly centered.
[x] Lock gripper before IK computation (gripper toggled separately)
[x] fix gripper after joint control, it stops working

# AI
